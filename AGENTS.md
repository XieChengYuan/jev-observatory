# Jev 活动看板：用户确认的约束

- 这是独立、通用的 Jev 活动看板，不与任何业务绑定。完整链路为输入 → 调用工具/判断要求及可观察阶段 → 原始返回 → 返回类型 → 对应结果池。候选名称和结构从 MCP 声明与返回动态生成。
- 看板是被动 MCP 观察器。运行数据只来自 MCP 代理真实记录的调用、返回、阶段、错误与用量。
- 业务收录、编辑复核、研究脚本或助手写入的任务状态不得覆盖可视化或池子计数。
- 内容采集期间不得修改看板 UI、操作回放按钮来冒充实时处理，或直接写入生产调用记录和统计。是否有调用、是否成功、模型返回什么，都以代理记录为准。
- 所有完成调用自动按持久游标排队展示，刷新或快速连续返回不能漏掉调用。调用次数与模型返回项数分开标注。
- 保留四类路由、固定顶部统计；输入内容区域上下滚动、不分页，输出、候选项和调用列表各自分页。未经用户要求不改整体 UI。不能出现整页滚动，输入区域之外不新增内部滚动框。
- 验证可用隔离测试数据；生产链路验证只能发起真实 MCP 调用，并明确标注验证用途，不把它们写入玩法网站。

- 右上角监听开关控制代理是否记录新调用，状态持久保存、所有进程共享。停止时保留历史并收齐已接收调用的结果；停止期间的新调用正常执行但不记录、不补录。
